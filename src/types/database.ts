/**
 * Placeholder database types for InsForge.
 * These will be replaced with generated types once the schema is defined.
 */
export interface Database {
  public: {
    Tables: Record<string, never>;
    Views: Record<string, never>;
    Functions: Record<string, never>;
  };
}
