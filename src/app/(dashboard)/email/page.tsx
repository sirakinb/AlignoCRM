import { MessagingSection } from "@/components/messaging/messaging-section";

export default function EmailPage() {
  return (
    <MessagingSection
      channel="email"
      title="Email"
      subtitle="Campaigns, templates, and sending settings."
    />
  );
}
